# Kibana 보안 설정 (프로덕션 환경에서는 강력한 무작위 값으로 변경하세요)
KIBANA_REPORTING_KEY=example-reporting-key-change-me
KIBANA_SECURITY_KEY=example-security-key-change-me

# Elasticsearch 설정
ES_JAVA_OPTS=-Xms512m -Xmx512m -Djna.tmpdir=/tmp -Des.enforce.bootstrap.checks=false

# Logstash 설정
LS_JAVA_OPTS=-Xmx256m -Xms256m

# 포트 설정
ELASTICSEARCH_PORT=9200
KIBANA_PORT=5601
LOGSTASH_BEATS_PORT=5044
LOGSTASH_TCP_PORT=5046
LOGSTASH_API_PORT=9600

# 인증 설정 (프로덕션 환경에서는 변경하세요)
ELASTIC_PASSWORD=changeme
ELASTIC_USERNAME=elastic